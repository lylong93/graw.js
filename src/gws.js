import core from './core'
import wgl from './wgl'
/**
 * main gws class
 */
class Gws {
    constructor(obj,config) {
        this.obj = obj 
        this.config = config
    }
    //转化
}

export default Gws